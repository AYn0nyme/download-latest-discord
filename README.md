# Install Discord from tar.gz
Downloads and install Discord to chosen path (updates if already installed)

## Usage
```sh
./download-latest-discord <PREFIX>
# If path is not provided, then installs to /opt/Discord
```

## Building
No dependencies are required, except [Go](https://go.dev)
```sh
go build .
```
